#ifndef FUZZYMODULE_H
#define FUZZYMODULE_H
#include <iostream>

class fuzzymodule
{
public:
    fuzzymodule(double triangle);
   static std::string fuzzyoutput(double triangle);
};

#endif // FUZZYMODULE_H
